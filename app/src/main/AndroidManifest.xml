<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yassen.mossab.mossabtaskmanger">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MyCarsActivity"></activity>
        <activity android:name=".AddCar" />
        <activity
            android:name=".MainTasksActivity"
            android:label="@string/title_activity_main_tasks"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MossabTaskManger"></activity>
        <activity android:name=".SignIn">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUP" />
        <activity android:name=".Sitting" />
        <activity android:name=".TempAllTaskActivity" />
    </application>

</manifest>